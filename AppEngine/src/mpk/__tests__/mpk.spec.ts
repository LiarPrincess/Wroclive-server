/*
// import Mpk from '../mpk-client';
// import logger from '../../util/__fakes__/logger';
// import { MpkApiType } from '../mpk-api';
// import { TimestampedLines, MPKVehicle, Line } from '../types';

// class MpkApiMock implements MpkApiType {
//   vehicles: MPKVehicle[] = [];

//   getVehicleLocations(lines: string[]): Promise<MPKVehicle[]> {
//     return Promise.resolve([]);
//   }

//   downloadLatestGtfsFile(path: string): Promise<void> {
//     return Promise.resolve();
//   }
// }

// const lines: Line[] = [
//   { name: 'A', type: 'bus', subtype: 'express' },
//   { name: '4', type: 'tram', subtype: 'normal' }
// ];

describe('mpk', () => {
  // 1. no lines -> does nothing
  // 2. invoke api
  //  2.1. no vehicles -> does nothing
  //  2.2. api throws -> expect throw
  //  2.3. got unrequested line -> ignore
  // 3. got vehicles
  //  3.1. not previously -> angle 0.0
  //  3.2. previously near -> angle as before
  //  3.3. previously far -> new angle
  it('is placeholder test', function() {
  });
});
*/
